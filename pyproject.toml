[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "minipython"
version = "0.1.0"
description = "MiniPython compiler for CS441"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
